<a href="/my/projects/new" class="btn btn-large btn-primary">Launch New Project</a>
</br></br></br>

  <% @projects.each do |project| %> 
  <div class="project">
    <li><%= link_to project.title, edit_my_project_path(project.id) %></li>
  </div>
  <% end %> 