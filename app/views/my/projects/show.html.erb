<h1><%= @project.title %></h1>

<p class="lead"><%= @project.teaser %></p>

<p><%= @project.description %></p>

<hr />

<h3>Goal: $<%= @project.goal %></h3>
<div class="progress progress-success progress-striped">
  <div class="bar" style="width: 35%;">35% Funded</div>
</div>

<hr />

<%= link_to "Edit Project", edit_my_project_path(@project) %> | <%= link_to "Delete Project", my_project_path(@project), method: :delete, confirm: "Are you sure you want to delete this project?" %>

